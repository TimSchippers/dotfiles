- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.zshrc:
    ~/.zshenv:
    ~/.zprofile:
    ~/.gitconfig:

- create:
    - ~/code

- shell:
    -
      command: ./setup_homebrew
      stdin: true
      stdout: true
      stderr: true

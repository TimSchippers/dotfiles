- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/code

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]

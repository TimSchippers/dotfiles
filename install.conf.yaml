- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc

- create:
    - ~/code

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
